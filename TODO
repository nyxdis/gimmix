* Support markup for current playlist.
* On closing gimmix, it should check if tray icon is visible, if not,
  then gimmix should quit. [suggested by qball]
* fix GtkComboBox in library->search
* port to gtk3
* port to libmpdclient
* parse arguments via glib
